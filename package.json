{
    "name": "azureextensionui",
    "description": "Used for unit tests in this package.",
    "version": "0.0.1",
    "aiKey": "test-ai-key",
    "publisher": "ms-azuretools",
    "main": "./extension",
    "engines": {
        "vscode": "^1.23.0"
    },
    "activationEvents": [
        "*"
    ],
    "contributes": {
        "commands": [
            {
                "command": "extension.signIn",
                "title": "Sign in to new account (clearSessionPreference=true)"
            },
            {
                "command": "extension.parallelGetSessions",
                "title": "Test parallel silent getSession() API calls"
            }
        ]
    }
}
